; prints binary when given a decimal number
; input is given in r1
printbin push sp,r2
	push sp,r0
	load r0,=32
	loop sub r0,=1
		load r2,r1
		shr r2,r0
		and r2,=1
		out r2,=crt
		comp r0,=0
		jgre loop
	pop sp,r0
	pop sp,r2
	exit sp,=0
